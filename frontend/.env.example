# Environment Variables for Nexus Frontend Deployment

# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key

# API URLs (adjust based on your backend deployment)
VITE_API_URL=http://localhost:5000
VITE_LOCAL_API_URL=http://localhost:5000
VITE_STREAM_KEY=your_getstream_key

# Other Service Keys (if applicable)
VITE_STREAM_VIDEO_API_KEY=your_stream_video_api_key
VITE_GEMINI_API_KEY=your_gemini_api_key

# Feature Flags (if needed)
VITE_ENABLE_RECORDING=true
VITE_ENABLE_TRANSCRIPTION=true

# Note: On Vercel, add these environment variables in the "Environment Variables" 
# section of your project settings.
# On Render, add them in the "Environment" section when setting up your service.
